export const HOUSEHOLD_ID = 'pol-andrea'

export function householdPath() {
  return `households/${HOUSEHOLD_ID}`
}

export function menusPath() {
  return `households/${HOUSEHOLD_ID}/menus`
}

export function plannerSlotsPath() {
  return `households/${HOUSEHOLD_ID}/plannerSlots`
}

export function shoppingItemsPath() {
  return `households/${HOUSEHOLD_ID}/shoppingItems`
}